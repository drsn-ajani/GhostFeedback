export const metadata = {
  title: 'GhostFeedback | Authentication',
  description: 'Sign in or create an account to start receiving anonymous feedback.',
}

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
